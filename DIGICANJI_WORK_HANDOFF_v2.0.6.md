# DigiCanji 開発引継ぎ — v2.0.6

更新日: 2026-08-21  
現行成果物: `DigiCanji_v2.0.6.zip`  
基準版: `DigiCanji_v2.0.5.zip`

## 1. 最初に把握すること

DigiCanji は、学校学習向けの手書き漢字・かな認識クイズと、配布用の単一HTMLを生成するエディターです。PC/スマホ、横書き・縦書きワイド・縦書きポータブルを対象とします。

プレイヤーの正本は `kanji_sample.html` です。`editor.html` の `PLAYER_TEMPLATE_B64` は必ず正本とバイト単位で同期してください。プレイヤーを修正したあと、この同期を忘れると、エディターから生成したHTMLだけ旧版になります。

今回のZIPは引継ぎ用なので、通常のGitHub公開5ファイルにこの引継ぎ書を加えた6ファイル構成です。

## 2. v2.0.6 の目的と品質基準

ユーザーの実機評価は v2.0.5 時点で **94点**。現時点の認識品質を下限として扱い、広い順位変更や正解依存の救済を行わず、失敗時に限って働く補助処理だけを追加しました。

今回の実機画像では、次の代表例が確認されています。

- `歩` を標準的に書いているのに `乳` と認識される。
- `衆` を標準的に書いているのに `腔` と認識され、少し書き直すと `衆` になる。
- `仁` はほぼ同じ筆跡でも、ある書き方では `?`、わずかな修正後は `仁` になる。
- `直` は一般的な手書き形を複数試しても `?` になることがある。
- 追加の実機画像でも、「候補なし」「同画数だが全体構造の異なる別字」「わずかな端点・比率差で結果が変わる」という同じ類型が見られた。

スクリーンショットからは元の筆画座標を完全には復元できないため、各画像をピクセル単位で再現して修正したわけではありません。以下の共通原因へ一般化して対処しています。

## 3. 今回一般化した原因

### A. 端点・比率差で正しい候補自体が消える

v2.0.5 以前にも、各画の端点を少量除いた中心軌跡を使う「第3変形」はありました。しかし、この第3照合は「すでに候補が残っているが弱い／経路間で揺れる」場合が中心で、生座標と通常正規化の両方で候補が消えた場合には必ずしも使われませんでした。

このため、`仁`・`直`のように字の中心骨格は正しいのに、端点の長さ、はね・止めの位置、全体比率の小差で外部認識器から候補が落ちるケースが残っていました。

### B. 同画数の別字が外部候補上位に来ると、全体構造の矛盾が弱く扱われる

既存の字体比較には「実際に書かれた1画が候補字形から大きく外れている」場合の拒否規則があります。一方で、活字体と手書きの差を許容するため字体減点には上限があり、1本ずつは中程度の不一致でも、**複数の実画が候補字形の対応箇所を持たない**候補を十分強く退けられない場合がありました。

`歩→乳`、`衆→腔` はこの類型として扱います。画数一致だけで採用せず、複数画にまたがる構造矛盾を確認します。

## 4. v2.0.6 で実装した変更

### 4.1 候補なし時だけ「中心骨格＋正規化」の第3照合を実施

`shouldTryRobustHanConfirmation()` を追加しました。

- 読み問題・フリーモードでは追加しない。
- 生座標／通常正規化後にも有効候補がない場合、端点を少量除いた第3変形で Google 手書き認識を1回だけ再確認する。
- すでに強い候補が確定している通常成功例では追加通信しない。
- 従来の弱い候補・経路不一致時の第3照合も維持する。

これにより、全漢字へ閾値を緩めるのではなく、「候補供給が途切れた失敗経路」だけを補います。

### 4.2 複数画にまたがる字形矛盾を一般判定

`evaluateDisplacedStrokeEvidence()` を拡張しました。

- 1画だけの局所的不一致は従来規則のまま。
- **7画以上**の候補について、十分な長さの実画が2本以上あり、その複数画が候補字形から同時に離れている場合を `multiStrokeSuspicious` とする。
- 複数画の合計比率と、各画の被覆率・平均距離・外側率／その画を除いたときの改善量がさらに厳しい条件を満たす場合だけ `multiStrokeHardReject` とする。
- 疑わしい段階では即座に捨てず、第3照合と教材内ローカル漢字候補の照合を起動する。
- **1〜6画の漢字には新しい複数画 hard reject を適用しない。** 少画数文字は端点や接触差の影響が大きいため、既存の許容度を維持する。

これは `歩` や `衆` を文字固有に優遇する規則ではなく、同画数の大きく異なる別字全般へ使える構造矛盾判定です。

### 4.3 既存の認識順位を保護

今回、次は変更していません。

- 正解文字を候補へ注入する処理（追加していない）。
- 問題の正解を使った候補順位操作（追加していない）。
- 外部認識器の通常候補順位重み。
- 全漢字共通の字体 hard reject 閾値の緩和。
- 画数、部品、交差、運筆方向など既存の対照規則。
- 小書きかなの位置判定。
- バックグラウンド認識・確定待ち・UI処理。

v2.0.3 で品質を大幅に落とした「正解文字を全漢字共通で再注入する救済」は復活させないでください。

## 5. v2.0.5 から維持する文字種対応

v2.0.5 で、エディターとプレイヤーの文字種判定を Unicode `Script=Han` / `Script=Hiragana` ベースへ広げています。以下を維持します。

- `々`、`〇`、`〻`。
- BMP外CJK統合漢字（例: `𠮷`）。
- `〆`、`〼`。
- 縦組み用かな反復記号 `〱 〲 〳 〴 〵`。
- カタカナ反復記号 `ヽ ヾ`。
- `ヵ ヶ`。
- 長音符 `ー`。

句読点や一般記号を無制限に許可する仕様ではありません。

## 6. 自動検証と制約

リリース作成時に必ず次を確認すること。

- `kanji_sample.html` の JavaScript 構文検査。
- `editor.html` の JavaScript 構文検査。
- 内蔵問題数 800 問の維持。
- `editor.html` の `PLAYER_TEMPLATE_B64` と `kanji_sample.html` のバイト単位一致。
- player/editor の version 表示が v2.0.6 で統一されていること。
- v2.0.6 追加自己試験:
  - 候補なし漢字では端点除去第3照合を使う。
  - 強い通常候補では第3照合を増やさない。
  - 複数画の字体矛盾では第3照合を起動する。
  - 同条件で教材内ローカル漢字照合を起動する。

この開発環境では `file://` および localhost のブラウザー実行が組織ポリシーで遮断されるため、今回のブラウザー内蔵自己試験の全件実行はできません。構文・埋込一致・問題数などの静的検証は実施し、実機の認識結果は次項で確認してください。

## 7. 次の実機確認で最優先する項目

1. 今回の画像と同じ問題で `歩` が `乳` へ流れないか。
2. `衆` が `腔` へ流れず、少し異なる標準的筆跡でも安定するか。
3. `仁` が端点・長さの小差で `?` になりにくくなったか。
4. `直` の一般的な手書き形を複数試し、候補なしが改善したか。
5. v2.0.5 で良好だった文字の回帰がないか。特に `受・残・礼・阪・的・自・合`、`り`、小書きかな。
6. `々`、`〇`、`〆`、`ヶ` など v2.0.5 の文字種対応が維持されているか。
7. Android Chrome の認識枠・見直し・答え合わせ一覧のタッチドラッグ、縦書きポータブル、長い問題文、確定表示に回帰がないか。

評価94点を基準にし、今回の4代表字が改善していても他の正常字が悪化する場合は採用しない方針とします。

## 8. 開発再開手順

1. このZIPを展開し、`kanji_sample.html` をプレイヤー正本として扱う。
2. 認識ロジックを変更した場合は、まず個別例ではなく共通原因へ一般化できているか確認する。
3. `kanji_sample.html` のJavaScript構文検査を行う。
4. `editor.html` の `PLAYER_TEMPLATE_B64` を最新 `kanji_sample.html` から再生成する。
5. editor の title/meta/version を同じバージョンへ更新する。
6. 800問維持、埋込プレイヤーのバイト一致を確認する。
7. 実機では新しい失敗例だけでなく、既存の高精度回帰セットも必ず確認する。
8. 問題の正解文字を判定器へ注入・優遇する変更は避ける。

## 9. 認識診断

公開URL末尾に `?recognitionDebug=1` を付けると、設定画面から認識診断JSONを出力できます。スクリーンショットだけでは元の筆画座標、外部候補列、各候補の拒否理由を完全再生できません。今後、同じ文字で再現性の低い誤認識が残る場合は、スクリーンショットに診断JSONを併用すると、次の調整をより安全に行えます。
